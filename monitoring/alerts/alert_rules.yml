groups:
  - name: ExampleAlerts
    rules:
      - alert: ExampleHighLoad
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Instance is down"
