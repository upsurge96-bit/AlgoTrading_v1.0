# -------------------------------------------------------------
# 🐍 Base Python Image
# -------------------------------------------------------------
FROM python:3.12-slim

# -------------------------------------------------------------
# 📂 Working Directory
# -------------------------------------------------------------
WORKDIR /app

# -------------------------------------------------------------
# 🧩 Copy Only Required Files
# -------------------------------------------------------------
COPY ./services/auth-service /app
COPY ./core /app/core
COPY ./config /app/config
COPY .env /app/.env

# -------------------------------------------------------------
# 🧠 Environment
# -------------------------------------------------------------
ENV PYTHONPATH=/app \
    PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    DOCKER_ENV=true

# -------------------------------------------------------------
# 📦 Install Dependencies
# -------------------------------------------------------------
RUN pip install --no-cache-dir -r /app/requirements.txt

# -------------------------------------------------------------
# 🚀 Run Token Refresher API
# -------------------------------------------------------------
CMD ["uvicorn", "token_refresher_service:app", "--host", "0.0.0.0", "--port", "8018"]
